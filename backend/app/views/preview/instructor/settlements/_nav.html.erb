<aside class="admin-preview-nav">
  <div class="admin-preview-nav__brand">
    <p class="eyebrow">Preview instructor</p>
    <h2>정산 확인 흐름</h2>
    <p>강사가 월정액 시청 기여 정산과 첨삭 정산을 어떻게 확인하게 될지 검토하는 시안입니다.</p>
  </div>

  <nav class="admin-preview-nav__links" aria-label="강사 정산 미리보기 메뉴">
    <%= link_to "정산 요약", preview_instructor_settlements_path, class: "admin-preview-link #{current_page?(preview_instructor_settlements_path) ? 'is-active' : ''}" %>
    <%= link_to "월별 상세", preview_instructor_settlement_month_path, class: "admin-preview-link #{current_page?(preview_instructor_settlement_month_path) ? 'is-active' : ''}" %>
  </nav>

  <div class="admin-preview-nav__meta">
    <div class="admin-preview-mini">
      <span>이번 달 예상 지급</span>
      <strong><%= number_to_currency(@final_payout, unit: "₩", precision: 0) %></strong>
    </div>
  </div>
</aside>
