<aside class="admin-preview-nav">
  <div class="admin-preview-nav__brand">
    <p class="eyebrow">Preview instructor</p>
    <h2>첨삭 처리 흐름</h2>
    <p>강사가 들어와서 대기 중인 첨삭 요청을 보고, 실제 피드백을 남기는 작업 흐름을 확인하는 영역입니다.</p>
  </div>

  <nav class="admin-preview-nav__links" aria-label="강사 첨삭 메뉴">
    <%= link_to "첨삭 대기열", preview_instructor_coaching_queue_path, class: "admin-preview-link #{current_page?(preview_instructor_coaching_queue_path) ? 'is-active' : ''}" %>
    <%= link_to "첨삭 검토", preview_instructor_coaching_review_path, class: "admin-preview-link #{current_page?(preview_instructor_coaching_review_path) ? 'is-active' : ''}" %>
  </nav>

  <div class="admin-preview-nav__meta">
    <div class="admin-preview-mini">
      <span>대기 요청</span>
      <strong><%= @queue_items.size %>건</strong>
    </div>
  </div>
</aside>
